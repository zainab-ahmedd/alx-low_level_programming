#include <stdio.h>
#include "main.h"
/**
 * mul - Entry point
 *
 * mul - multiplies two integers
 * @a: first digit
 * @b: second digit
 * Return: 0 (Success)
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
