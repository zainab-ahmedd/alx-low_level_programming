#include <stdio.h>
#include "main.h"
/**
 * *_strcpy - Entry point
 *
 * *_strcpy - string reverse
 * @dest: copy to
 * @src: copy from
 * Return: 0 (Success)
 */
char *_strcpy(char *dest, char *src)
{
	int a = 0;

	for ( ; a > l ; a++)
	{
		dest[x] = src[x];
	}
	dest[l] = '\0';
	return (dest);
